{
  "description": "GNN tuned for crossing pair — wider gate, lower confirm threshold for 30-50km targets",
  "tracker_type": "GNN",
  "filter_model": "IMM",
  "volume": 1e9,
  "beta": 1e-14,
  "params": {
    "gate": 80,
    "far_gnn": 1e-6,
    "confirm_threshold": 8,
    "delete_threshold": -3,
    "gate_jpda": 80,
    "far_jpda": 1e-6,
    "far_mht": 1e-6,
    "beta_jpda": 1e-14,
    "time_tolerance_jpda": 0.05,
    "num_tracks_jpda": 500,
    "tomht_threshold_multiplier": [0.2, 1, 1],
    "max_branches": 5
  },
  "TUNING_NOTES": {
    "gate": "Widened 45→80: targets at 30-50km have larger measurement noise, need wider association gate",
    "confirm_threshold": "Lowered 20→8: 4.8s scan period with only ~2 dets/scan means fewer score opportunities",
    "delete_threshold": "Raised -5→-3: delete stale tracks faster to free up capacity"
  }
}
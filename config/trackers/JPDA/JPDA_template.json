{
  "INSTRUCTIONS": [
    "Copy this file, rename it, tune parameters.",
    "Reference in a run file as: trackers/JPDA/my_JPDA",
    "",
    "This file contains JPDA-SPECIFIC algorithm tuning only.",
    "Shared environment params (volume, beta, Pd, filter) live in:",
    "  config/trackers/tracker_globals.json"
  ],
  "PARAM_DOCS": {
    "tracker_type": "GNN | JPDA | TOMHT — which tracking algorithm to use.",
    "filter_model": "CV = straight line assumption. IMM = handles turns (recommended).",
    "gate_jpda": "JPDA association gate. Larger=more permissive. 30-120 typical.",
    "far_jpda": "False alarm rate for JPDA. 1e-6 typical.",
    "beta_jpda": "JPDA clutter density parameter. 1e-14 typical.",
    "time_tolerance_jpda": "Time window for grouping detections. 0.05s typical.",
    "num_tracks_jpda": "Max tracks for JPDA. 500 default.",
    "confirm_threshold": "Score to confirm a track. 5-20 typical.",
    "delete_threshold": "Score below which track is deleted. -3 to -10 typical."
  },
  "description": "Joint Probabilistic Data Association — soft assignment, real-time",
  "tracker_type": "JPDA",
  "filter_model": "IMM",
  "params": {
    "gate": 45,
    "far_gnn": 1e-6,
    "confirm_threshold": 20,
    "delete_threshold": -5,
    "gate_jpda": 45,
    "far_jpda": 1e-6,
    "far_mht": 1e-6,
    "beta_jpda": 1e-14,
    "time_tolerance_jpda": 0.05,
    "num_tracks_jpda": 500,
    "tomht_threshold_multiplier": [0.2, 1, 1],
    "max_branches": 5
  }
}
{
  "INSTRUCTIONS": [
    "Copy this file, rename it, edit params.",
    "Reference in a run file as: sensors/SSR/my_SSR"
  ],
  "PARAM_DOCS": {
    "frequency_hz": "Operating frequency in Hz. SSR default is 1.03e9 (L-band). SSR uses cooperative transponder — rain attenuation matters less, but frequency still affects clutter and Doppler.",
    "rpm": "Must match co-located PSR for synchronized scanning.",
    "pd": "0.99 — SSR uses cooperative transponder, very reliable.",
    "far": "1e-7 — very low, transponder replies are discrete.",
    "rangeLimits": "120nm — transponder has own TX, longer range than PSR.",
    "refRCS": "Reference RCS (m^2). SSR uses transponder, not skin return."
  },
  "name": "Secondary Surveillance Radar (MSSR/IFF)",
  "type": "SSR",
  "platform": "tower",
  "frequency_hz": 1.03e9,
  "params": {
    "rpm": 12.5,
    "fov": [1.4, 10],
    "tilt": 2,
    "pd": 0.99,
    "far": 1e-7,
    "rangeLimits": [0, 25000],
    "rangeRes": 100,
    "refRCS": 20,
    "mountingLoc": [0, 0, -15]
  }
}
